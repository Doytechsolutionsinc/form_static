{
  "name": "metrotex-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "render-build": "npm install --build-from-source sqlite3 && npm install"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0"  // <-- THIS IS THE MISSING LINE!
                                // Use the latest stable version if you prefer, e.g., "^13.0.0"
    // "sqlite3": "^5.0.11",      // <--- If you're not using SQLite in your current server.js, you can remove this.
    // "path": "^0.12.7",         // <--- If not directly using Node's 'path' module, can remove.
    // "uuid": "^9.0.0",          // <--- If not generating UUIDs directly, can remove.
    // "express-basic-auth": "^1.2.0" // <--- If not using basic auth middleware, can remove.
  },
  "engines": {
    "node": ">=18.x"
  }
}
